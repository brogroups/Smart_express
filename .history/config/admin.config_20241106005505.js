const UserModel = require('../models/user.model')

module.exports.adminCreateConfig = (event) => {
    try {
        const 
    } catch (error) {
        return { status: 500, error: 'Server Error' }
    }
}
